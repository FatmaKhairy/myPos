@extends('layouts.dashboard.app')@section('content')    <div class="content-wrapper">        <section class="content-header">            <h1>@lang('site.edit category')</h1>            <ol class="breadcrumb">                <li><a href="{{ route('dashboard.welcome') }}"><i class="fa fa-dashboard"></i> @lang('site.dashboard')</a></li>                <li><a href="{{ route('dashboard.categories.index') }}"> @lang('site.categories')</a></li>                <li class="active">@lang('site.Edit')</li>            </ol>        </section>        <section class="content">            <div class="box box-primary">                <div class="box-header">                    <h3 class=" box-title">@lang('site.Edit')</h3>                </div> <!---end of box header-->                <div class="box-body">                    @include('partials._errors')                    <form action="{{route('dashboard.categories.update',$category->id)}}" method="post">                        {{csrf_field()}}                        {{method_field('put')}}                        @foreach (config('translatable.locales') as $local)                            <div class="form-group">                                {{--site.ar/en.name--}}                                <label>@lang('site.'.$local.'.name')</label>                                <!--ar[name] الاسم هو اللي ليه اكتر من قيمه فهو اللي array-->                                {{--$category->$local.['.name']--}}                                <input type="text" name="{{$local}}[name]" class="form-control"  value="{{$category->translate($local)->name}}" >                            </div>                        @endforeach                        <!----custom/permision table -->                        <!--custom tap-->                        <!--end custom tap-->                        @if(auth()->user()->hasPermission('update_categories'))                            <div class="form-group">                                <button type="submit" class="btn btn-primary" ><i class="fa fa-plus">@lang('site.Edit')</i></button>                            </div>                        @else                            <button type="submit" class="btn btn-primary disabled" ><i class="fa fa-plus">@lang('site.Edit')</i></button>                        @endif                    </form>                </div> <!---end of box body-->            </div><!---end of box -->        </section>    </div>@endsection